# 选择题
# 1、进程由哪三部分组成？（）
A、`PCB`、程序段、`CPU`

B、`PCB`、数据段、`JCB`

C、`DCB`、`FCB`、`JCB`

D、`PCB`、数据段、程序段

# 2、（）是进程唯一存在的标志
A、`PCB`

B、`JCB`

C、`DCB`

D、`FCB`

# 3、进程和程序的一个本质区别是（）
A、前者分时使用`CPU`，后者独占`CPU`

B、前者存储在内存中，后者存储在外存

C、前者在一个文件中，后者在多个文件中

D、前者动态的，后者静态的

# 4、进程中由（）到执行是由进程调度引起的
A、挂起

B、激活

C、就绪

D、完成

# 5、某进程要求的一次打印输出结束，该进程被（）
A、阻塞

B、执行

C、唤醒

D、挂起

# 6、不可能发生的转换是（）
A、`就绪->执行`

B、`执行->就绪`

C、`就绪->阻塞`

D、`阻塞->就绪`


# 7、正在执行的进程由于时间片用完被暂停执行，则变为（）状态
A、静止阻塞

B、活动阻塞

C、静止就绪

D、活动就绪

# 8、处于静止阻塞的进程，进程等待事件结束后会变为（）状态
A、静止阻塞

B、活动阻塞

C、静止就绪

D、活动就绪

# 9、进程处于执行状态时，因终端的请求而暂停下来以便研究其运行情况，则进程变为（）状态
A、静止阻塞

B、活动阻塞

C、静止就绪

D、活动就绪

# 10、进程由活动就绪变为静止就绪，应该利用（）原语
A、`active`

B、`suspend`

C、`block`

D、`wakeup`

# 11、进程有静止就绪变为活动就绪，应利用（）原语
A、`block`

B、`suspend`

C、`active`

D、`wakeup`

# 12、进程由阻塞状态变为就绪状态应利用（）原语
A、`block`

B、`suspend`

C、`active`

D、`wakeup`

# 13、不属于`CPU`现场信息的是（）
A、指令计数器

B、进程的就绪、阻塞、执行等基本状态

C、堆栈的栈顶指针

D、段表控制寄存器

# 14、不属于`PCB`内容的是（）
A、进程打开文件的描述符表

B、进程调度信息

C、程序段、数据段的内存基址和长度

D、完整程序代码

# 15、从用户态转为系统态通过（）实现
A、执行进程修改程序状态字

B、中断屏蔽

C、访管指令或中断

D、进程调度


# 16、两个进程`A`和`B`共享一临界资源，`A`正处于临界区执行，则（）
A、`A`执行不能被中断，即临界区的代码具有原子性

B、`A`执行能被中断，但中断后不能将`CPU`调度给`B`

C、`A`执行能被中断，只要`B`就绪，就可以将`CPU`调度给`B`

D、`A`执行能被中断，只要`B`就绪，就必定将`CPU`调度给`B`


# 17、对于记录型信号量，在执行一次`wait`时，信号量的值应（）
A、减指定数值

B、加1

C、减1

D、加指定数值

# 18、对于记录型信号量，执行`signal`时，若其值（），应唤醒阻塞队列中的进程
A、大于0

B、大于1

C、小于1

D、小于0

# 19、生产者-消费者问题中，应设置资源信号量`full`为（）
A、`+n`

B、`0`

C、`-n`

D、`1`


# 20、在引入线程的操作系统中，资源分配和调度的基本单位是（），`CPU`调度和分派的基本单位是（）

A、进程  线程

B、进程  进程

C、线程  线程

D、线程  进程


# 填空题
# 1、程序并发执行比顺序执行产生的新特征为（）、（）和（）
# 2、引入进程的目的是（），引入线程的目的是（）
# 3、进程由（）、（）和（）组成，其中（）是进程存在的唯一标志
# 4、进程最基本的特征为（）和（），除此之外还有（）和（）特征
# 5、进程实质是程序的一次执行，因此具有（）的基本特征，该特征还表现在进程由（）而产生，由（）而执行，由（）而消亡
# 6、用户为了阻止进程继续运行，应利用（）原语，若进程正在执行，则变为（）状态，若用户需要恢复，利用（）原语，进程变为（）状态
# 7、进程同步主要是对多个相关进程在（）上进行协调
# 8、同步机制应该遵顼的准则有（）、（）、（）和（）
# 9、记录型信号量中，`S.value>0`时的值表示（），每次`wait`操作意味着（），因此将其值（），当`S.value`（）`0`时，进程应阻塞
# 10、记录型信号量中，`signal`操作意味着（），因此将`S.value`（），当`S.value<=0`时，表示（），此时应该（）
# 11、利用信号量实现进程互斥时，应将（）置于（）与（）之间
# 12、每个进程中访问（）的那段代码称为临界区，为实现对临界区的共享，应保证（）地进入临界区，为此在每个进程的临界区前应设置（），临界区后应设置（）
# 13、利用共享的文件进行进程通信的方式称作（），除此之外，进程通信的类型还有（）、（）和（）三种类型
# 14、客户机——服务器系统通信机制主要的实现方法是（）、（）和（）三种
# 15、为实现消息缓冲队列通信，`PCB`应增加（）、（）和（）
# 16、引入线程后，操作系统以（）作为资源分配的基本单位，（）为`CPU`调度和分派的基本单位
# 17、在采用用户级线程的系统中，`OS`进行`CPU`调度的对象是（），采用内核支持的线程系统中，`CPU`调度的对象是（）
# 18、线程之所以能减少并发执行的开销是因为（）


# 论述题
# 1、比较进程与程序的异同
# 2、`PCB`的作用是什么？为什么说`PCB`是进程存在的唯一标志
# 3、何谓“忙等”？有什么缺点？
# 4、如何保证进程互斥地访问临界资源？
# 5、消息缓冲队列通信机制应该具备哪几方面的功能？
# 6、比较直接通信方式和间接通信方式
# 7、从调度性、并发性、拥有资源、独立性、系统开销以及对多处理机的支持比较进程和线程
# 8、什么是内核支持线程和用户级线程？对两者进行比较
